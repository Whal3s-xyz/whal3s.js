module.exports = {
    preset: 'jest-playwright-jsdom',
    transform: {
        '^.+\\.ts?$': 'ts-jest',
    },
    transformIgnorePatterns: ['<rootDir>/node_modules/'],
    testEnvironment: "jsdom"
};
